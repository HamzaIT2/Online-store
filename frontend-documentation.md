# التوثيق الهندسي الشامل لمشروع Frontend - "الدوار العراقي"

## 1. نظرة عامة على الهيكلية (Architecture Overview)

### المفاهيم الأساسية لـ React المستخدمة في المشروع

#### **المكونات (Components)**
في هذا المشروع، React مبني على هيكلية **مكونات قابلة لإعادة الاستخدام** التي تشبه مكعبات الليغو. كل مكون هو دالة JavaScript ترجع JSX (صيغة تشبه HTML).

- **المكونات الوظيفية** : جميع مكونات المشروع هي دوال JavaScript حديثة
- **الـ Props** : البيانات تنتقل من المكونات الأب إلى الأبناء عبر الـ props (الخصائص)
- **إعادة الاستخدام** : مكونات مثل `ProductCard`، `FavoriteToggle`، و `EmptyState` تستخدم في صفحات متعددة

#### **إدارة الحالة (State Management)**
المشروع يستخدم عدة طرق لإدارة الحالة:

1. **useState Hook** : للحالة المحلية داخل المكونات
   ```javascript
   const [products, setProducts] = useState([]);
   const [loading, setLoading] = useState(false);
   ```

2. **useContext Hook** : للحالة العامة مثل الثيم
   ```javascript
   const { darkMode, toggleDarkMode } = useTheme();
   ```

3. **localStorage** : لاستمرارية البيانات (سلة المشتريات، تفضيلات المستخدم)
   ```javascript
   localStorage.setItem('cart', JSON.stringify(cartItems));
   ```

#### **الـ Hooks المخصصة**
المشروع يشمل hooks مخصصة لوظائف معقدة:
- `useChatSocket.js` : لإدارة اتصالات WebSocket في الوقت الفعلي
- `useSocket.js` : للاتصالات socket العامة

#### **التوجيه (Routing)**
المشروع يستخدم **React Router DOM** للتنقل:
- **BrowserRouter** : يغلف التطبيق للتوجيه
- **Routes & Route** : تحدد المسارات والمكونات المرتبطة بها
- **ProtectedRoute** : مكون يحمي المسارات التي تتطلب مصادقة
- **useNavigate** : للتنقل البرمجي
- **useLocation** : للوصول إلى معلومات الرابط الحالي

## 2. تدفق البيانات والاتصال بالخادم (Data Flow & API Integration)

### خطوة بخطوة: كيف يتواصل Frontend مع Backend

#### **1. إعداد Axios**
المشروع يستخدم `axiosInstance.js` كنقطة مركزية لجميع اتصالات API:

```javascript
const axiosInstance = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL || "/api/v1",
});
```

#### **2. اعتراضات الطلبات (Request Interceptors)**
قبل كل طلب، المعترض يضيف تلقائياً توكن المصادقة:

```javascript
axiosInstance.interceptors.request.use((config) => {
  const token = localStorage.getItem("token");
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});
```

#### **3. اعتراضات الاستجابة (Response Interceptors)**
الاستجابات تتم معالجتها للتعامل مع أخطاء 401 (غير مصرح) واستخراج البيانات:

```javascript
axiosInstance.interceptors.response.use(
  (response) => {
    // يستخرج البيانات من الهيكل { success: true, data: ... }
    if (response.data?.success && response.data?.data) {
      return response.data.data;
    }
    return response.data;
  },
  (error) => {
    // يتعامل مع أخطاء 401 وينظف المصادقة
    if (error.response?.status === 401) {
      localStorage.removeItem('token');
      localStorage.removeItem('user');
    }
    return Promise.reject(error);
  }
);
```

#### **4. خدمات API المركزية**
كل مجال وظيفي لديه خدمة API الخاصة به:

- **authAPI.js** : المصادقة (login, register, logout)
- **productsAPI.js** : إدارة المنتجات (CRUD, بحث, تصفية)
- **favoritesAPI.js** : إدارة المفضلة
- **messagesAPI.js** : المراسلة والدردشة
- **ordersAPI.js** : إدارة الطلبات

#### **5. إدارة حالات التحميل (Loading States)**
في كل مكون، حالات التحميل تتم إدارتها بأنماط متسقة:

```javascript
const [loading, setLoading] = useState(false);
const [error, setError] = useState("");

const loadProducts = async () => {
  setLoading(true);
  setError("");
  try {
    const data = await getAllProducts(filters, searchTerm);
    setProducts(data?.data || []);
  } catch (err) {
    setError(t('error_loading_product'));
  } finally {
    setLoading(false);
  }
};
```

#### **6. معالجة الأخطاء**
الأخطاء تعرض للمستخدم برسائل مترجمة:

- **رسائل الخطأ** : تستخدم دالة `t()` للدولجة
- **Snackbars** : للإشعارات المؤقتة
- **Empty States** : للحالات التي لا توجد فيها بيانات

#### **7. تحديث الواجهة**
بعد عمليات API، الواجهة تحدث عبر:
- **أحداث مخصصة** : `window.dispatchEvent(new Event('cart:updated'))`
- **إعادة تحميل البيانات** : استدعاءات متكررة لوظائف التحميل
- **التنقل البرمجي** : `navigate('/success-page')`

## 3. تفصيل المجلدات والملفات (Detailed File Breakdown)

### هيكل مجلد `src/`

#### **`pages/` - الصفحات الرئيسية**

**`Home.jsx`** - الصفحة الرئيسية
- **الوظائف** : عرض المنتجات، شريط البطل، البحث، التصفية
- **الحالات** : المنتجات، التحميل، الفلاتر، مصطلح البحث
- **الـ Hooks المستخدمة** : `useEffect`، `useState`، `useNavigate`، `useLocation`
- **التكاملات** : API المنتجات، شريط ديناميكي، أحداث مخصصة

**`ProductDetails.jsx`** - تفاصيل المنتج
- **الوظائف** : عرض كامل للمنتج، الدردشة مع البائع، الإضافة للسلة
- **الحالات** : المنتج، التحميل، الرسائل، الصور
- **الإجراءات** : التواصل مع البائع، الإضافة للسلة، المفضلة

**`Cart.jsx`** - سلة المشتريات
- **الوظائف** : إدارة سلة localStorage، حساب الإجمالي، الدفع
- **الاستمرارية** : تستخدم `localStorage` لحفظ السلة
- **تكامل الدفع** : نافذة الدفع مع ZainCash و COD

**`Login.jsx` / `Register.jsx`** - المصادقة
- **الوظائف** : نموذج المصادقة، التحقق، إعادة التوجيه
- **التكاملات** : Clerk للمصادقة الاجتماعية، API الخلفية

**`Profile.jsx`** - الملف الشخصي
- **الوظائف** : عرض الملف الشخصي، التعديل، الإحصائيات
- **الحالات** : بيانات المستخدم، التحميل، التعديل

**`MyProducts.jsx`** - منتجات المستخدم
- **الوظائف** : قائمة منتجات المستخدم، عمليات CRUD
- **الإجراءات** : التعديل، الحذف، الترويج

#### **`components/` - المكونات القابلة لإعادة الاستخدام**

**`ProductCard.jsx`** - بطاقة المنتج
- **الوظائف** : عرض مدمج للمنتج، إجراءات سريعة
- **الحالات** : snackbar، اللغة، الصور
- **الإجراءات** : التنقل، الإضافة للسلة، المراسلة، المفضلة
- **التجاوب** : أحجام صور متكيفة حسب الشاشة

**`Navbar.jsx`** - شريط التنقل
- **الوظائف** : التنقل الرئيسي، البحث، الملف الشخصي للمستخدم
- **الحالات** : القائمة المحمولة، العدادات (السلة، المفضلة)، الملف الشخصي
- **التكاملات** : الثيم، اللغة، البحث، المصادقة

**`FavoriteToggle.jsx`** - زر المفضلة
- **الوظائف** : الإضافة/الحذف من المفضلة مع API
- **الحالات** : حالة المفضلة، التحميل
- **API** : `favoritesAPI.js`

**`PaymentModal.jsx`** - نافذة الدفع
- **الوظائف** : عملية دفع متعددة الخطوات
- **الطرق** : ZainCash، الدفع عند الاستلام
- **الحالات** : الخطوة الحالية، معلومات الدفع، التحقق

**`ProductPaymentDialog.jsx`** - حوار دفع المنتج
- **الوظائف** : دفع خاص لمنتج معين
- **الحالات** : طريقة الدفع، معلومات التوصيل، التحقق
- **التكامل** : ZainCash provider

**`EmptyState.jsx`** - الحالة الفارغة
- **الوظائف** : عرض عندما لا توجد بيانات
- **الأنواع** : cart، favorites، products، messages
- **التخصيص** : العنوان، الوصف، الإجراء

#### **`api/` - خدمات API**

**`axiosInstance.js`** - إعداد HTTP مركزي
- **الوظائف** : مثيل Axios مع اعتراضات
- **المصادقة** : توكن تلقائي في الـ headers
- **معالجة الأخطاء** : 401 handling، تنظيف localStorage
- **التصحيح** : دوال مساعدة للـ console

**`productsAPI.js`** - API المنتجات
- **الدوال** : `getAllProducts`، `getProductById`، `createProduct`، `updateProduct`، `deleteProduct`
- **المعلمات** : الفلاتر، البحث، التصفح

**`authAPI.js`** - API المصادقة
- **الدوال** : Login، register، logout، refresh token

**`favoritesAPI.js`** - API المفضلة
- **الدوال** : `getMyFavorites`، `toggleFavorite`، `checkFavorite`

#### **`context/` - السياقات العامة**

**`ThemeContext.jsx`** - إدارة الثيم
- **الوظائف** : الوضع المظلم/الفاتح
- **الاستمرارية** : localStorage
- **الـ Hooks** : `useTheme()`، `ThemeProvider`

#### **`hooks/` - الـ Hooks المخصصة**

**`useChatSocket.js`** - Socket للدردشة
- **الوظائف** : اتصال WebSocket في الوقت الفعلي
- **الأحداث** : الرسائل، حالة الاتصال، الأخطاء

**`useSocket.js`** - Socket عام
- **الوظائف** : إدارة اتصال socket أساسي

#### **`i18n.js`** - التدويل (Internationalization)

**الهيكل** : قاموس ثنائي اللغة (عربي/إنجليزي)
- **الدوال** : `getLang()`، `toggleLang()`
- **الترجمات** : أكثر من 400 مفتاح ترجمة
- **الأحداث** : `languageChanged` لإعادة عرض المكونات

### تدفق البيانات المحدد

#### **سلة المشتريات (Cart)**
1. **الإضافة** : `ProductCard` -> `localStorage` -> Event `cart:updated`
2. **الاستمرارية** : `localStorage.setItem('cart', JSON.stringify(items))`
3. **المزامنة** : الاستماع لأحداث التحديث
4. **التنظيف** : إفراغ تلقائي عند تسجيل الخروج

#### **المفضلة (Favorites)**
1. **التبديل** : `FavoriteToggle` -> `favoritesAPI.toggleFavorite()`
2. **التحديث** : إعادة تحميل تلقائية للعداد
3. **الاستمرارية** : Backend API + cache محلي

#### **المصادقة**
1. **تسجيل الدخول** : `Login.jsx` -> `authAPI.login()` -> `localStorage` token
2. **التحقق** : `ProtectedRoute` -> يتحقق من التوكن
3. **تسجيل الخروج** : تنظيف `localStorage` + إعادة التوجيه

## 4. نقاط قوة المشروع (Project Strengths)

### **1. الدعم ثنائي اللغة المتقدم (i18n)**
- **تنفيذ كامل** : أكثر من 400 ترجمة عربي/إنجليزي
- **تبديل اللغة الديناميكي** : تغيير اللغة في الوقت الفعلي
- **دعم RTL/LTR** : اتجاه تلقائي حسب اللغة
- **تحديثات مدفوعة بالأحداث** : إعادة عرض تلقائية للمكونات
- **الاستمرارية** : التفضيل محفوظ في localStorage

**المزايا التقنية** :
- استخدام Custom Events للتواصل بين المكونات
- إدارة اتجاهات RTL/LTR مع Material-UI
- بدائل للترجمات المفقودة

### **2. هيكلية دفع معقدة**
- **طرق دفع متعددة** : ZainCash (محفظة محمولة) والدفع عند الاستلام
- **سير عمل متعدد الخطوات** : عملية دفع في 4 خطوات
- **نمط Provider** : `ZainCashProvider` للتجريد
- **إدارة الحالة** : إدارة معقدة لحالات الدفع
- **معالجة الأخطاء** : تحقق في كل خطوة

**المزايا التقنية** :
- تصميم نمط Strategy لطرق الدفع
- تحقق قوي من النماذج
- إدارة الحالات غير المتزامنة
- تكامل مع واجهات برمجية خارجية

### **3. إدارة حالة هجينة**
- **استمرارية localStorage** : سلة المشتريات وتفضيلات المستخدم
- **Context API** : الثيم العام
- **الـ Hooks المخصصة** : Socket والوظائف المعقدة
- **هيكلية مدفوعة بالأحداث** : التواصل بين المكونات
- **تحسين الأداء** : تجنب إعادة العرض غير الضروري

**المزايا التقنية** :
- مزيج مثالي من استراتيجيات مختلفة لإدارة الحالة
- استمرارية بيانات المستخدم
- تواصل فعال بين المكونات
- قابلية التوسع لوظائف مستقبلية

### **4. هيكلية API قوية**
- **إعداد مركزي** : `axiosInstance` مع اعتراضات
- **مصادقة تلقائية** : حقن التوكن الشفاف
- **معالجة الأخطاء** : إدارة مركزية لأخطاء 401
- **تحويل الاستجابة** : توحيد استجابات API
- **أدوات التصحيح** : دوال تصحيح مدمجة

**المزايا التقنية** :
- فصل الاهتمامات (Separation of Concerns)
- كود قابل لإعادة الاستخدام والصيانة
- معالجة قوية للأخطاء
- سهولة الاختبار والتصحيح

### **5. تصميم متجاوب متقدم**
- **تكامل Material-UI** : نظام تصميم متسق
- **نقاط التجاوب** : تكيف محمول/تابلت/كمبيوتر
- **صور متكيفة** : أحجام الصور حسب الشاشة
- **دعم RTL** : دعم كامل للغة العربية
- **الوضع المظلم** : ثيم مظلم/فاتح

**المزايا التقنية** :
- استخدام احترافي لـ Material-UI
- أداء محسن مع صور متكيفة
- إمكانية الوصول و UX عالية الجودة
- دعم متعدد اللغات كامل

---

## خلاصة

هذا المشروع frontend يظهر إتقانًا متقدمًا لـ React وأفضل ممارسات التطوير الحديث. الهيكلية مصممة جيدًا، قابلة للتوسع والصيانة. النقاط القوية مثل الدعم ثنائي اللغة، هيكلية الدفع المعقدة، وإدارة الحالة الهجينة تظهر فهمًا عميقًا للمفاهيم المتقدمة لـ React وتطوير الويب الحديث.

الكود منظم جيدًا، يتبع مبادئ SOLID، ويستخدم أنماط التصميم المناسبة. التكامل مع Material-UI، إدارة الـ API، وتحسين الأداء يجعله مشروعًا على مستوى احترافي جاهز للإنتاج.
